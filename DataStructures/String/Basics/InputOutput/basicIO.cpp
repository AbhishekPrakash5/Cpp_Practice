#include <bits/stdc++.h>
using namespace std;

int main(){
    string str;
    getline(cin,str); // if you use cin anywhere then use cin.ignore() after that cin to use getline(cin,variable);
    cout<<str<<endl;
}