#include<iostream.h>
#include<conio.h>

void chkprime(int n)
{
	int flag = 1;
		for(int i = 2; i <= n/2; i++)
		{
			if ( n % i == 0 )
			{
				flag = 0 ;
				break ;
			}
		}
	if(flag)
		cout<<"prime";
	else
		cout<<"not prime";
	
}
void main()
{
	int a;
	cout<<"e";
	cin>>a;
	chkprime(a);
	getch();
}